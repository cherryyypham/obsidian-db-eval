# Optional. sync.yaml is the primary configuration; these are only read
# by load_vault.py when it is run directly instead of through `obsidian`.
VAULT_PATH=/Users/cherryyypham/NotakingHub
DATABASE_URL=postgresql:///notakinghub_hw10
